providers = ["python"]

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "pip install uv",
  "cd backend && uv sync --frozen --no-dev"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
